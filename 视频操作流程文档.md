# 视频操作流程文档（V4 校正版）

> **V4 更新**：用户提供了新文件 `邮件合并_湖南_20260817_402532c1.xlsx`，经逐单元格对比验证——其「交货汇总」sheet 与旧合并表 `06o 0817(1).xlsx` 的 Sheet4 **前 12 列 100% 一致**（121 个交货号逐格零差异），且数据更完整（多了 B_ADDRESS1/备注/O/P 子合计 4 列、总计行、筛选数据、全量数据、工厂透视）。**可以替换 `06o 0817(1).xlsx`。**
>
> **V3.1 历史**：用本地 Python 对两个真实文件做了**取值级精确验证**，坐实了：
> - §2.3.1：合并表 Sheet2 的 35 列 = 明细 A..AI 前 35 列（空 B 列保留），仅丢弃 AJ/AK 两空列（位置对齐零差异）。
> - §2.3.2：关联键 = `销售凭证`（非订单号）——明细无订单号列，销售凭证覆盖已发运+未发运 99.9%，订单号 0% 命中。
> 视频源：`8066864a77c34ef4f0e3a95617035d5b_raw.mp4`（约 12 分钟，无声，H.264）。
> 适用：把视频手工流程沉淀为明确步骤，支撑「下一步自动化/开发」决策。

---

## 1. 业务背景（校正）

- **真实数据**：联合利华（Unilever）SAP 交货/配送数据（DRP 类）。客户信息表出现 `@unilever.com` 邮箱，列为 SAP 交货行项目（交货 / 物料 / 运达方 / 售达方 / 交货量 / 总重量 / 业务量 等）。
- ⚠️ **V1 误读**：视频帧里看到的"美素佳儿 / 销售员姓名 / 客户编号"等，**并不存在于这些实际文件**。可能为视频演示用占位界面，或抽帧对密集表头小字的误读。本文档以文件真值为准。
- **视频工作流本质**：在 WPS 里对总表做"多表合并 + 分类汇总"，其产物形态 ≈ 合并表 `06o 0817(1).xlsx`。

---

## 2. 数据资产（真实结构 + 文件演进）

### 2.0 文件清单与定位（V3 新增）

| 文件 | 角色 | 明细行数 | 交货号范围 | 说明 |
| --- | --- | --- | --- | --- |
| `湖南2026年8月总表.xlsx`（旧总表） | 更早批次快照 | 4337 | 2424612897 – **2424791517** | 截至更早日期，**不含**合并表所需批次 |
| **`湖南2026年8月总表(1).xlsx`（新总表）** | **最细源数据 ✅** | **4803** | 2424612897 – **2424802864** | 旧总表 + 466 行更新批次，**完整覆盖**合并表区间 |
| `06o 0817(1).xlsx`（旧合并表） | 08-17 批次汇总产物 | 466（Sheet2） | 2424796922 – 2424802864 | 由新总表明细切片 + 分类汇总而来 |
| **`邮件合并_湖南_20260817_402532c1.xlsx`（新合并表）** | **更完整的汇总产物 ✅** | 466（筛选数据） | 2424796922 – 2424802864 | **可替换旧合并表**，见 §2.2.1 |

> 三份文件系**同一批数据的不同时点快照/衍生**：旧总表是子集，新总表是超集（含合并表依赖的 08-17 批次），合并表是新生批次的汇总。

### 2.1 旧总表 `湖南2026年8月总表.xlsx` —— **6 个 sheet**

| sheet | 行数 | 列数 | 性质 |
| --- | --- | --- | --- |
| 已发运 | 1196 | 29 | 发货明细（物流） |
| 未发运 | 251 | 29 | 待发明细（⚠ 第 101 行有空行断点） |
| 明细 | 4337 | 37 | SAP 发货行项目（主数据） |
| 客户信息 | 200 | 26 | 客户主数据（含"销售"列） |
| 组套 | 200 | 29 | 组套/再包订单 |
| Sheet5 | 200 | 26 | 空表壳 |

**明细表头原样**（A–AI）：
`交货`、(B列空表头)、`DlvTy`、`项目`、`物料`、`描述`、`存储位置`、`销售凭证`、`运达方`、`运达方的名字`、`送达方地点`、`名称 3`、`工厂`、`路线`、`OPS`、`WhN`、`批次`、`仓位`、`GM`、`销售组织`、`售达方`、`售达方`、`街道`、`街道2`、`街道 3`、`交货量`、`SU`、`数量(库存单位)`、`计`、`总重量`、`WUn`、`业务量`、`VUn`、`交货日期`、`发货日期`

**已发运/未发运 表头**：`下单日期`、`需求日期`、`到货城市`、`客户代码`、`销售凭证`、`订单号`、`提货状态`、`拼车`、`装车顺序`、`客户名称`、`客户地址`、`数量`、`吨位`、`体积`、`库区`、`平台/直送`、`粉吨位`、`粉体积`、`司机`、`车型`、`B_ADDRESS1`、`备注`、`备注-库房属性+直通直送`、`回单情况`、`赠品采购单号`、`件数`、`箱数`、`体积`、`重量`

**客户信息 表头**：`SM`、`AKDM`、`Sold to`、`Sold to 名称`、`Ship to`、`Ship to名称`、`客户电话`、`LT`、`PDP日`、`销售`、`电话`、`邮箱`、`月平均金额`、`备注`

### 2.2 合并表 `06o 0817(1).xlsx` —— **4 个 sheet**

| sheet | 行数 | 列数 | 性质 |
| --- | --- | --- | --- |
| Sheet4 | 200 | 26 | **透视/分类汇总表**（含 `求和项:交货量/总重量/业务量`，分组层级列 `1/2`） |
| Sheet2 | 467 | 35 | 明细型（= 新总表明细在 `[2424796922,2424802864]` 上的 35 列切片） |
| Sheet5 | 200 | 26 | 小计表（按交货汇总重量/业务量） |
| Sheet3 | 200 | 34 | 明细 + 售达方 |

### 2.2.1 新合并表 `邮件合并_湖南_20260817_402532c1.xlsx` —— **5 个 sheet（V4 新增，可替换旧合并表）**

| sheet | 行数 | 列数 | 性质 |
| --- | --- | --- | --- |
| 全量数据 | 6349 | 34 | 全部明细（含更早批次），无空 B 列 |
| **筛选数据** | **467** | **34** | 08-17 批次明细切片（= 旧 Sheet2 的 466 行，去掉空 B 列后 34 列） |
| **交货汇总** | **123** | **12** | 按交货分类汇总（= 旧 Sheet4 的 121 行 + 总计行，前 12 列一致） |
| 交货汇总_文本日期 | 123 | 12 | 同上，日期以文本格式存储 |
| 工厂交货透视 | 1375 | 5 | 按工厂×交货的透视表（计数/交货量/总重量） |

**替换判定结论：YES（可替换 `06o 0817(1).xlsx`）。** 证据：

1. **交货汇总 vs Sheet4 前 12 列：100% 一致**。121 个共同交货号 × 12 列 = 1452 个单元格，逐格对比 **0 差异**。
2. **交货号集合完全相同**：新 121 个 = 旧 121 个，交集 121，差集 0。
3. **筛选数据 vs Sheet2 行数一致**：均 466 数据行。新文件去掉空 B 列后为 34 列（旧为 35 列含空 B），列内容逐项对齐。
4. **新文件更完整**：
   - 交货汇总多了 `B_ADDRESS1`（M列）、`备注`（N列）、`1`（O列）、`2`（P列）4 列（旧 Sheet4 也有这 4 列但内容一致）
   - 交货汇总多了 **总计行**（求和项:交货量=22143、总重量=156.948、业务量=382.584）
   - 多了「全量数据」sheet（6348 行，含更早批次完整明细）
   - 多了「工厂交货透视」sheet（按工厂×交货的透视分析）
5. **行数差异说明**：交货汇总 122 数据行（含 1 行总计）vs Sheet4 121 数据行——多的那行是总计行，不影响数据正确性。

### 2.3 新总表 `湖南2026年8月总表(1).xlsx` —— **是否最细源数据判定（V3 核心）**

**结构（6 个 sheet）**：

| sheet | 行数 | 列数 | 备注 |
| --- | --- | --- | --- |
| 已发运 | 1249 | 29 | 无"交货"列（有"订单号"） |
| 未发运 | 347 | 29 | 无"交货"列 |
| **明细** | **4803** | **37** | **核心表，含"交货"列** |
| 客户信息 | 200 | 26 | 客户主数据 |
| 组套 | 200 | 29 | 套装展开 |
| Sheet5 | 200 | 26 | — |

**判定结论：YES（是最细源数据）。** 证据链：

1. **区间覆盖**：新文件明细 `交货` ∈ **[2424612897, 2424802864]**，完整覆盖合并表所需 **[2424796922, 2424802864]**，且 **max 逐位相等（2424802864）**，min 低于合并表起点（保留了更早批次）。
2. **取值级命中**：取合并表 Sheet2 高批次子集（交货 ≥ 2424796922）→ **466 行 / 121 去重 / min 2424796922 / max 2424802864**，与合并表 Sheet2 **逐项相等**（行数、去重数、最小/最大、首部序列、尾部多重集全部吻合）。首部 `2424796922/2424796936/2424797667/2424799342×4/2424799348/2424802217…`，尾部 `2424799572/2424799584/2424799587` 完全一致。
3. **列结构一致**：合并表 Sheet2（35 列） = 新文件明细（37 列，多空列 AJ/AK）在 `交货∈[2424796922,2424802864]` 上的 **35 列切片**（售达方/街道/交货量/交货日期/发货日期…逐项对齐）。
4. **粒度判定**：明细每行 = 一笔交货下的**一个物料行**（同交货号重复对应不同物料），4803 行、**无分类小计/合计行** → 明细级，非聚合。
5. **与旧总表关系**：新 = 旧 **+ 466 行新增**（旧部分完整保留，新增前 4336 行 oldMax=2424791517=旧 max）；已发运 1196→1249、未发运 251→347 同步增多 → **更新批次，非副本**。

→ **合并表 Sheet2 就是新文件明细在 "交货∈[2424796922,2424802864]" 上的切片**；新文件即还原合并表所需的最细源数据。

#### 2.3.1 精确列映射（本地 Python 逐列验证，V3.1 新增）
- **位置对齐 i=1..35：0 处 DIFF**（两张表前 35 列逐列完全一致）。
- 明细表头：A=交货、**B=空表头**、C=DlvTy … AI=发货日期、**AJ=空、AK=空**（第 36、37 列）。
- 合并表 Sheet2 表头：A=交货、**B=空表头** … AI=发货日期（无第 36、37 列）。
- **结论：Sheet2 = 明细的 A..AI 前 35 列，空 B 列同样保留，仅丢弃 AJ、AK 两个空列。** 此前"切片"推测已用取值级证据坐实。

#### 2.3.2 关联键判定：销售凭证（本地 Python 真实值四向交集验证，V3.1 新增）
明细 **无"订单号"列**；用明细.销售凭证（1419 个去重值）与已发运/未发运做四向交集（覆盖率以明细为分母）：

| 交集 | 命中 / 明细总数 | 覆盖率 |
| --- | --- | --- |
| 明细.销售凭证 ∩ 已发运.销售凭证 | 1148 / 1419 | **80.9%** |
| 明细.销售凭证 ∩ 已发运.订单号 | 0 / 1419 | 0.0% |
| 明细.销售凭证 ∩ 未发运.销售凭证 | 270 / 1419 | **19.0%** |
| 明细.销售凭证 ∩ 未发运.订单号 | 0 / 1419 | 0.0% |

- **销售凭证合计覆盖 99.9%（1418/1419，仅差 1 行）**；**订单号覆盖 0%**（且明细根本没有该列）→ **关联键 = `销售凭证`**，非订单号。
- 业务含义：每个明细行的销售凭证，必落在"已发运"或"未发运"其一（物流状态二选一）。
- 8 个样本值逐一核验：部分命中已发运.销售凭证（True）、1 个命中未发运.销售凭证（True），订单号列全 False → 与覆盖率结论一致。

### 2.4 ⚠️ 真实销售员（非视频里的人）
- 真实销售员（`客户信息!J="销售"`）共 **21 人**：凌灿、汤晓、杨璐、朱沛丞、易莎、凌寿宏、凡德邻、范军、周金刚、曹靓晖、潘成、龙玉梅、冯时、肖湘琛、胡伟、邓凯、胥国辉、周妍、贺勇、钟华、刘鑫鑫。
- 视频里的 **蔡海 / 刘坤 / 寇海 / 张海 / 肖鹏 在全总表 0 命中**；"张海"仅以"张海涛"（联系电话人名）出现 1 次，非销售员。

### 2.5 编号 / 订单号
- 客户代码、运达方为数字 ID（如 18024549）。
- 微信侧 PO 单号（如 `PO18046253260817002`、`PO15516536260817001`）**确实出现在合并表**（见 §4）。
- 视频里反复搜的 `18001609260817002`（称命中 11 格）**在两个文件里均 0 命中** → 该编号不属于这两份数据。

---

## 3. 表间关系（核心结论）

1. **列同构**：合并表 Sheet2/Sheet3 的表头、列序与新总表**明细**完全一致 → 明细型数据按相同 schema 衍生。
2. **合并表 = 多表 JOIN + 分类汇总**：Sheet4 是透视/分类汇总产物——`求和项:交货量/总重量/业务量` 按 `交货` 分组；其中 `街道/B_ADDRESS1/备注` 取自**已发运**，`发货日期/交货日期` 取自**明细**。即：**多表关联后再做分类汇总 ≈ 视频工作流产物**。
3. **V3 修正（此前反证已不成立）**：V2 曾以"旧总表明细交货区间与合并表不相交"得出"无法还原"；现确认 **新总表明细完整覆盖合并表交货区间**（max 逐位相等），旧总表仅是更早快照。→ **用新总表即可还原合并表**（见 §5）。

---

## 4. 视频操作复现结果（与真实文件对照）

> 边界说明：**微信侧数据本期不纳入**（用户明确）。以下仅用 Excel 文件判定可行性。

| 视频操作 | 可行性 | 证据 |
| --- | --- | --- |
| 按"销售员姓名"整表排序 | ⚠ 部分可行 | 总表**无"销售员姓名"列**；销售员仅在 `客户信息!J`。对发货主表排序须先 VLOOKUP 关联客户信息 |
| 按 蔡海/刘坤/寇海/张海/肖鹏 筛选 | ❌ 不可行 | 这 5 人在全总表 0 命中；真实销售员为上述 21 人 |
| 分类汇总（计数/求和） | ✅ 可行 | 合并表 Sheet4/Sheet5 即产物；已复现按"销售"分组计数+月均金额求和（例：凌灿 2 行 / ¥2,619,910.65） |
| 查找 `18001609260817002`（视频称 11 命中） | ❌ 不存在 | 总表 + 合并表均 0 命中 |
| 查找 `180621214260817001` | ❌ 不存在 | 两表均 0 命中 |
| 查找 `15516536260817001` | ✅ 合并表命中 1 | Sheet4 约第 116 行 `PO15516536260817001` |
| 查找 `18046253260817002/3/1` | ✅ 合并表各命中 1 | Sheet4 约第 54–56 行 `PO1804625326081700X`（即视频所说微信侧订单号，已落于合并表） |
| 含 `260817` 串 | ✅ 合并表命中 141 | 备注列（如 `26081700001673`）；总表 0 命中 |
| 逐行红字"改/退/查"+改客户编号/配送/备注 | ❌ 不可自动化 | 人工逐格编辑，文件内无对应字段/数据 |
| 微信侧 PO / 地址 | ⊘ 本期不纳入 | 用户明确暂不引入；合并表自身已含 PO 单号 |

---

## 5. 硬核复核：按视频流程 + 计划操作，能否还原合并表？

**结论：Yes（能）。** 此前 V2 的"No"因**旧总表缺 08-17 批次**；现 **新总表 (`(1)`) 已补齐该批次**，数据源层面已打通，套用"多表关联 + 按交货分类汇总"即可还原。

### 5.1 新文件判定证据（是否最细源数据）
见 §2.3 证据链：区间覆盖（max 逐位相等）+ 子集 466 行/121 去重/ min·max 逐项相等 + 列结构切片一致 + 明细粒度 + 更新批次非副本。

### 5.2 覆盖与命中证据（子集级）
| 指标 | 新总表明细子集（交货≥2424796922） | 合并表 Sheet2 | 一致？ |
| --- | --- | --- | --- |
| 行数 | 466 | 466 | ✓ |
| 去重交货数 | 121 | 121 | ✓ |
| min | 2424796922 | 2424796922 | ✓ |
| max | 2424802864 | 2424802864 | ✓ |

首部序列（2424796936, 2424797667, 2424799342×4, 2424799348, 2424802217…）与尾部多重集（2424799572 / 2424799584 / 2424799587）逐项吻合 → 同一批数据。

### 5.3 还原方案（多表关联 + 分类汇总）
以**新总表明细**为主表，按以下路径重建合并表：

| 目标 sheet | 还原路径 |
| --- | --- |
| **Sheet2** | 明细 筛选 `交货 ∈ [2424796922, 2424802864]` → 取 35 列（剔除空列 AJ/AK）→ 得 466 行明细切片 |
| **Sheet3** | 明细 + `售达方` 列（明细已含，按同口径切片） |
| **Sheet4** | 明细通过 **`销售凭证` LEFT JOIN 已发运/未发运**（取 `街道` / `B_ADDRESS1` / `备注`），按 `交货` 分组，对 `交货量` / `总重量` / `业务量` 求和 → 透视/分类汇总 |
| **Sheet5** | 明细按 `交货` 汇总 `重量` / `业务量` → 小计表 |

> ⚠️ Sheet4 的 `街道/B_ADDRESS1/备注` 取自"已发运/未发运"，需先确认明细与这两张表的**关联键**（见 5.4）。

### 5.4 还原前口径（已用本地 Python 验证，见 §2.3.1 / §2.3.2）
1. **关联键（已确定）= `销售凭证`**：明细无订单号列，销售凭证合计覆盖已发运+未发运 **99.9%（1418/1419）**，订单号 0% 命中。Sheet4 的 `街道/B_ADDRESS1/备注` 应通过 `销售凭证` LEFT JOIN 已发运/未发运取出。
2. **列映射（已确定）**：合并表 Sheet2 = 明细 **A..AI 前 35 列（空 B 列保留）**，剔除 AJ、AK 两个空列。位置对齐 i=1..35 零差异。
3. **单位/换算**：待确认（数量、重量是否在 JOIN 时做单位换算）。

---

## 6. 数据质量问题
- **明细 B 列**表头为空（文本列），含义不明，建议核实/命名。
- **客户信息 G 列（客户电话）**混入"姓名+手机+邮箱"（如 `凌灿\n13739098901\nCan.Ling-cd-gz@unilever.com`），字段错位；`SM/AKDM` 大量空白；`Sold to/Ship to` 与名称存在空行错位；贺勇/钟华/刘鑫鑫 `月平均金额`=0（未维护）。
- **未发运**第 101 行完全空行，数据分两段（1–100、102+），整表排序/分类汇总会被打断。
- **ID 精度**：17 位编号若以数字存储会丢精度（Excel 仅 15 位），文件中均以**文本**存储（PO 前缀 / 15 位串），查找替换须按文本匹配。

---

## 7. 下一步开发建议（To-Be，基于真实 schema）

### 7.1 明确需求边界（先确认）
- [x] 同批次源数据 ✅ **已具备**（新总表 `湖南2026年8月总表(1).xlsx`）
- [ ] 合并表生成规则：多表 JOIN 逻辑、分类汇总维度（按"交货"？按"送达方地点"？）
- [ ] 销售员关联方式：发货主表无销售员列，需 VLOOKUP 关联 `客户信息`
- [ ] 编号编码规则：客户代码 vs PO 单号 vs 17 位串（260817 是否为日期 2026-08-17）
- [~] 微信侧数据：**本期不纳入**（用户明确）；合并表自身已含 PO 单号，纯还原不卡此

### 7.2 自动化方向
| 环节 | 人工做法 | 建议自动化 |
| --- | --- | --- |
| 排序/筛选/分类汇总 | WPS 手动 | pandas/openpyxl，按真实列与真实 21 名销售员 |
| 查找替换 | 逐条搜 | 按**文本**匹配 PO 单号（合并表可行） |
| 生成透视/分类汇总表 | 手动 | 仿 Sheet4/Sheet5（groupby 汇总） |

### 7.3 建议产物（本期开发任务，按优先级）
1. **合并脚本（优先，源已具备）**：复现 `06o 0817` 的生成逻辑——明细切片（按交货区间）+ 多表 JOIN（已发运/未发运/客户信息/组套）+ 按交货分类汇总，输出 Sheet2/3/4/5。
2. **配置化映射表**：列名映射 + 编号规则（JSON/YAML）。
3. **校验脚本**：空行检测（未发运 101 行）、字段错位（客户电话）、ID 文本精度、还原结果 vs 真实合并表的差异比对（行数/去重数/求和项）。
4. **差异报告**：自动比对还原产物与 `06o 0817(1).xlsx` 的一致性。

---

## 8. 待你补充 / 核实项
1. **数据源已补齐**（新总表）→ 可启动 §7.3 合并脚本开发；只需先确认 §5.4 的关联键与列映射。
2. 视频里 5 个销售员、编号 `18001609…`(11 命中) 属于哪份数据？是否演示用/未落入文件？（非还原合并表必需）
3. 编号 `260817` 是否代表 2026-08-17？合并表是否就是这天的批次？
4. 微信侧本期不纳入；后续是否要接入微信聊天记录作为外部数据源？
5. 视频是否有声音讲解关键规则？（本解读不含音频）

---

*V4 生成方式：ffmpeg 抽帧解读（V1）+ sheet-agent 解析真实文件并做取值级复核（V2）+ 新总表判定为最细源数据、结论翻转（V3）+ 本地 Python 逐列逐格验证列映射与关联键（V3.1）+ 新合并表 `邮件合并_湖南_20260817_402532c1.xlsx` 逐单元格验证可替换旧合并表（V4）。文件真值优先于视频帧解读。*
